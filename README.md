# MorixOS Calculator 
MorixOS Calculator is a simple Python console application that allows you to perform basic arithmetic operations: addition, subtraction, multiplication, and division. It's designed to handle multiple numbers at once and provides a clean command-line interface.

## Features

- Add, subtract, multiply, or divide multiple numbers.
- Clear console between operations for a neat interface.
- Looping menu until the user decides to exit.
